apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-sql
  name: my-sql
spec:
  ports:
  - port: 3306
    protocol: TCP
    targetPort: 3306
  selector:
    app: my-sql
  type: ClusterIP
status:
  loadBalancer: {}
